# Zależności runtime: CLI + warstwa web (Vercel). Wersje potwierdzone działaniem.
# UWAGA: to jest też lista, którą instaluje Vercel dla funkcji serverless
# (api/index.py) — trzymamy ją szczupłą, bez reportlab/pytest (patrz requirements-dev.txt).
pandas==3.0.3
pdfplumber==0.11.10
RapidFuzz==3.14.5
XlsxWriter==3.2.9
PyYAML==6.0.1
typer==0.26.8
rich==15.0.0
flask==3.1.3
