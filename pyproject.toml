[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "transport-audit"
version = "0.1.0"
description = "Atrybucja i audyt kosztów transportu (MyBed / Delta Industries)"
requires-python = ">=3.11"
dependencies = [
    "pandas>=2.2",
    "pdfplumber>=0.11",
    "RapidFuzz>=3.0",
    "typer>=0.12",
    "rich>=13.0",
    "XlsxWriter>=3.2",
    "PyYAML>=6.0",
    "flask>=3.0",
]

[project.optional-dependencies]
# Vercel instaluje TYLKO [project.dependencies] z pyproject.toml (uv) —
# generowanie fikstur (reportlab/openpyxl) i testy trzymamy poza bundlem.
dev = ["pytest>=8.0", "reportlab>=4.0", "openpyxl>=3.1"]
supabase = ["supabase>=2.0"]

[project.scripts]
transport-audit = "transport_audit.cli:app"

[tool.setuptools.packages.find]
include = ["transport_audit*"]

[tool.pytest.ini_options]
testpaths = ["transport_audit/tests"]
filterwarnings = ["ignore::DeprecationWarning"]
